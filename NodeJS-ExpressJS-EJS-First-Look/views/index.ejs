<%- include('./partials/slider.ejs') %>

    
        <% if (products.length>0) { %>
            <div class="row">
                <% products.forEach(product=> { %>
                    <!-- <% productCard(product) %> -->
                    <!-- ProductCard Bilesenine product datasini gonderip render ediyoruz -->
                    <%- include('./partials/product-card.ejs',{product}) %>
                <% }) %>
            </div>
        <% } else { %>
                <div class="alert text-bg-warning text-center text-white mt-3">
                    <div class="card-header"><h4>Gösterilecek herhangi bir ürün bulunmamaktadır.</h4></div>
                </div>
        <% } %>
    <!-- Dosya icersinde fonksiyonlar bu sekilde kullanilir. Belirli metotlar neticesinde ekrana yansitilacak olan bilesen duzenlenebilir -->
    <!-- Bu yontemle birlikte bilesenin farkli bir klasorden her hangi bir baska sayfa icersine cagirmak da mumkundur -->
    <!-- Bunun icin include methoduna prop vererek datayi aktaraip sonra render edebilirz -->
                <!-- <% function productCard(product) { %>
                    <div class="col-md-4">
                        <div class="card my-3" >
                            <img src="/img/<%=product.image %>" class="card-img-top" alt=<%= product.description %>>
                            <div class="card-body">
                                <h5 class="card-title"><%= product.name %></h5>
                                <p class="card-text"><%= product.description %></p>
                            </div>
                            <div class="card-footer bg-dark text-white d-flex justify-content-between align-items-center">
                                <i class="uil uil-lira-sign" style="font-size: 18px;"><%= product.price %></i>
                                <button class="btn btn-outline-light btn-sm">Details  <i class="uil uil-arrow-from-right"></i></button>
                            </div>
                        </div>
                    </div>
                  <% } %> -->